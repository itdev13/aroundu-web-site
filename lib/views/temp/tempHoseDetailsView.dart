import 'package:flutter/material.dart';

class HouseDetailsView extends StatelessWidget {
  const HouseDetailsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('House Details'),
      ),
    );
  }
}